# News cover assets

Source images for `NewsCoverAssetsSeeder`. Published to `public/images/news/` (git-backed)
and mirrored to the Laravel `public` disk under `news/images/` when writable.

Do not store staff Filament uploads here — those live only on the durable volume/S3.
