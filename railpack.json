{
  "$schema": "https://schema.railpack.com",
  "build": {
    "buildCommand": "npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan storage:link 2>/dev/null || true && php artisan optimize && php artisan queue:work --sleep=3 --tries=3 --max-time=3600 & php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
  }
}
