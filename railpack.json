{
  "$schema": "https://schema.railpack.com",
  "build": {
    "buildCommand": "rm -rf node_modules 2>/dev/null || true && npm ci && npm run build && mkdir -p storage/app/public/governance && cp -r database/seeders/assets/* storage/app/public/governance/"
  },
  "deploy": {
    "startCommand": "bash railway/start.sh"
  }
}
